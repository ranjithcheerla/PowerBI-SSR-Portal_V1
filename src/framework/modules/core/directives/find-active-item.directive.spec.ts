import { FindActiveItemDirective } from './find-active-item.directive';

xdescribe('FindActiveItemDirective', () => {
  xit('should create an instance', () => {
    // const directive = new FindActiveItemDirective();
    // expect(directive).toBeTruthy();
  });
});
