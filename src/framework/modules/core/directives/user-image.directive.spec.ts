import { UserImageDirective } from './user-image.directive';

describe('UserImageDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new UserImageDirective();
  //   expect(directive).toBeTruthy();
  // });
});
